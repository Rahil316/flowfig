# examples

Shared fixtures consumed by `core`'s tests, and later by `figma-plugin`, `extension`, and `agent-kit` — keeps every producer honest against the same resolved/translated contract. Populated once `core`'s format/resolver/translator schema exists.
